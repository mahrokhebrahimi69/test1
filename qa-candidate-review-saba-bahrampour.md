# ارزیابی تسک QA — صبا بهرام‌پور

**تاریخ بررسی:** ۱۴۰۴/۰۵/۱۱  
**تسک:** اعمال کد تخفیف در پلتفرم سفارش آنلاین غذا  
**فرمت تحویل:** ۳ فایل PDF (بخش اول، دوم، سوم)

---

## خلاصه حکم

کاندیدا تسک را کامل پوشش داده، Assumptions خوبی گذاشته، و نشانه‌های تفکر تستی واقعی دارد. سطح کلی **قابل قبول تا خوب** است؛ برای نقش Junior/Mid QA مناسب، برای Senior نیاز به عمق بیشتر در API و اولویت‌بندی عملیاتی دارد.

**پیشنهاد:** دعوت به مصاحبه فنی (با چند سؤال عمیق روی Race Condition، Concurrent Apply، و تصمیم‌گیری Release).

---

## پوشش نسبت به خواسته‌های تسک

| بخش تسک | وضعیت | توضیح |
|--------|--------|--------|
| بخش ۱ — Functional | ✅ کامل | TS-F1 تا F3 |
| بخش ۱ — Negative | ✅ خوب | TS-N1 تا N3 |
| بخش ۱ — Edge Case | ✅ خوب | انقضا، سفارش لغو/مرجوع، حداقل مبلغ |
| بخش ۱ — Boundary | ✅ خوب | 199999 / 200000 / 200001 + سقف تخفیف |
| بخش ۱ — Validation | ✅ | Case sensitive، کاراکتر خاص |
| بخش ۱ — Error Handling | ✅ خوب | قطع اینترنت + Retry + خطای ۵۰۰ |
| بخش ۱ — UX | ✅ | غیرفعال بودن فیلد، حذف کد، breakdown قیمت |
| بخش ۱ — Security | ✅ قوی | Rate limit، دور زدن کلاینت، Concurrent |
| بخش ۱ — Performance | ✅ | Latency + Load |
| بخش ۲ — API Test Cases | ✅ | TC1–TC11 |
| بخش ۲ — Status Codes | ✅ | 200/400/401/403/409/429/500 |
| بخش ۲ — Validation | ✅ | Request و Response |
| بخش ۲ — Negative | ✅ | خوب و متنوع |
| بخش ۲ — Perf/Reliability | ✅ | Idempotency، Consistency، Rate Limit |
| بخش ۳ — اولویت‌بندی | ⚠️ متوسط | چارچوب درست، عمق عملیاتی کم |

---

## نقاط قوت

### ۱. Assumptions هوشمندانه (نشان تفکر واقعی QA)
قبل از طراحی سناریو، مفروضات را صریح کرده — این رفتار حرفه‌ای است و معمولاً نشان می‌دهد کاندیدا با ابهام Requirement کار کرده، نه فقط لیست کردن تست‌کیس:

- تخفیف درصدی ۲۰٪ با سقف ۱۰۰ هزار
- معیار شهر = آدرس تحویل (نه GPS/IP)
- حداقل مبلغ بدون هزینه ارسال و مالیات
- «اولین سفارش» = اولین سفارش موفق پرداخت‌شده با آدرس تهران؛ لغو/مرجوع مانع نیست
- انقضا بر اساس Server time
- اعمال فقط روی محصول (نه ارسال/مالیات)
- فقط پرداخت آنلاین؛ عدم ترکیب با محصول دارای تخفیف

این مفروضات دقیقاً جاهایی را پر می‌کنند که تسک مبهم گذاشته بود.

### ۲. Security قوی‌تر از حد انتظار تسک
- Rate Limiting / Brute Force
- جلوگیری از دور زدن شرط پرداخت در کلاینت (اعتباری بعد از اعمال کد)
- Concurrent apply از دو دستگاه برای یک کاربر (Race Condition)

این سه مورد نشان می‌دهد کاندیدا فراتر از Happy Path فکر کرده.

### ۳. ساختار منظم بخش اول
برای هر سناریو عنوان، Steps، Expected Result و Priority آمده. دسته‌بندی‌ها با خواسته تسک هم‌خوان است.

### ۴. API: Idempotency و Reliability
فرض Idempotency روی همان سفارش + Race Condition همزمان، و Consistency در DB، نشان درک خوبی از تست API واقعی است.

### ۵. بخش سوم: شروع درست با Clarification
ابتدا سؤال از PO/PM/Dev می‌پرسد (حیاتی بودن فیچر، Blocker، کمپین، زمان، اتومیشن) — این رفتار درست در فشار Release است.

---

## نقاط ضعف و ریسک‌ها

### بخش اول
1. **تکرار زیاد Steps** — تقریباً همه سناریوها preamble یکسانی دارند؛ می‌شد با Preconditions کوتاه‌تر نوشت. کیفیت تحلیل را پایین نمی‌آورد ولی نشان می‌دهد تجربه نگارش تست‌کیس قابل بهینه‌سازی است.
2. **سناریوی شهر غیرتهران صریح نیست** — قانون «فقط تهران» در Assumptions آمده ولی سناریوی Negative جدا برای آدرس کرج/اصفهان دیده نشد (یا خیلی کم‌رنگ است).
3. **یک کد در هر سفارش** — قانون اول Feature؛ سناریوی صریح «اعمال دو کد همزمان / جایگزینی کد دوم» در بخش UI ضعیف است (در API اشاره شده).
4. **TS-B3 مبلغ اعشاری** (`99,999.8`) — برای تومان عجیب است؛ ممکن است فرض درصد روی عدد فرد باشد، ولی بهتر بود روی ریال/تومان صحیح گرد شود و سناریوی Rounding جدا باشد.
5. **پیام‌های Expected Result فرضی** — طبیعی است، ولی بهتر بود مشخص کند این متن‌ها پیشنهادی‌اند نه از Spec.

### بخش دوم
1. **Conflict 409 برای کد منقضی** — معمولاً برای منقضی/نامعتبر، `400` یا `422` رایج‌تر است؛ `409` بیشتر برای state conflict (سفارش پرداخت‌شده، کد قبلاً مصرف‌شده روی سفارش دیگر) مناسب است. انتخاب Status Code کمی ناهماهنگ است.
2. **Response validation ناقص** — فیلد `discountAmount` در جدول Validation به‌صورت صریح با type/positive/consistency با order amount نیامده (در نمونه موفق هست، در چک‌لیست کم‌رنگ است).
3. **Response time < 100ms** — فرض خیلی سخت‌گیرانه و بدون context (شبکه، DB، geo). بهتر بود SLA واقع‌بینانه‌تر یا وابسته به محیط باشد.
4. **تست‌های قانون بیزنس در API کم‌رنگ** — مثلاً حداقل مبلغ، شهر تهران، first-order بودن کاربر به‌صورت TCهای شماره‌دار جدا کمتر آمده (بیشتر در Negative کلی جمع شده).

### بخش سوم
1. **کم‌عمق‌ترین بخش** — فقط حدود ۲ صفحه؛ چارچوب Risk-based درست است ولی مثال عملی روی همین ۵ فیچر + ۱۵ باگ ندارد.
2. **تفاوت Severity بالا / Priority پایین** را نام می‌برد ولی استراتژی مشخص (مثلاً defer با monitoring، یا hotfix بعد از release) را باز نمی‌کند.
3. **چه چیزی را تست نکند** کلی است (UI ظاهری، مرورگر قدیمی) — بدون tie به business risk واقعی.

---

## نشانه‌های استفاده از AI

تسک صراحتاً منع استفاده از AI داشته. ارزیابی الگو:

| نشانه | مشاهده |
|------|--------|
| Assumptions شخصی‌سازی‌شده و domain-specific | ✅ ضد الگوی generic AI |
| اعداد و قوانین اختراع‌شدهٔ منسجم (۲۰٪، سقف ۱۰۰هزار) | ✅ نشان تحلیل دستی |
| فارسی با غلط‌های تایپی OCR/نوشتاری طبیعی | ✅ |
| ساختار بخش‌بندی‌شده شبیه تمپلیت | ⚠️ هم AI هم QA واقعی این کار را می‌کنند |
| بخش ۳ نسبتاً کوتاه و کلی | ⚠️ اگر کل پاسخ AI بود معمولاً هر ۳ بخش پرحجم و متقارن می‌شدند |

**جمع‌بندی تشخیص AI:** احتمال استفادهٔ کامل از AI پایین است. پاسخ بوی «QA که Requirement را شکافته و فرض گذاشته» می‌دهد، نه خروجی یکنواخت ChatGPT. احتمال کمکی جزئی AI را نمی‌توان صفر کرد، ولی الگوی کلی authentic به نظر می‌رسد.

---

## امتیاز پیشنهادی (از ۱۰)

| معیار | امتیاز | توضیح |
|------|--------|--------|
| تحلیل Requirement و Assumptions | ۸.۵ | قوی‌ترین بخش |
| پوشش سناریو و دسته‌بندی‌ها | ۸ | تقریباً کامل |
| Edge / Boundary / Security | ۸ | Security برجسته |
| API Testing | ۷ | خوب؛ Status Code و SLA قابل بحث |
| اولویت‌بندی تحت فشار | ۶ | درست اما سطحی |
| وضوح و حرفه‌ای بودن نگارش | ۷ | منظم؛ کمی تکراری |
| **میانگین کلی** | **۷.۵ / ۱۰** | |

---

## سؤالات پیشنهادی برای مصاحبه

1. اگر دو کاربر مختلف هم‌زمان یک کد single-use عمومی را apply کنند، چه اتفاقی باید بیفتد و چطور تست می‌کنی؟
2. چرا برای کد منقضی `409` گذاشتی نه `400`/`422`؟
3. «اولین سفارش» را اگر کاربر سفارش موفق در شیراز داشته و حالا تهران سفارش می‌دهد چطور تفسیر می‌کنی؟ (فرض خودت را دفاع کن)
4. در Release با ۵ فیچر و ۱۵ باگ، یک ماتریس اولویت نمونه بکش (نه فقط تئوری).
5. اگر بعد از apply موفق، قبل از پرداخت، سبد تغییر کند (حذف آیتم و زیر حداقل برود) رفتار سیستم چیست؟

---

## حکم نهایی برای استخدام

| سطح نقش | توصیه |
|---------|--------|
| Junior QA | ✅ قوی — بالاتر از میانگین |
| Mid-level QA | ✅ قابل قبول؛ مصاحبه فنی تأیید کند |
| Senior / Lead QA | ⚠️ کافی نیست مگر مصاحبه عمق تصمیم‌گیری و API را جبران کند |

**Recommendation:** Proceed to technical interview.
